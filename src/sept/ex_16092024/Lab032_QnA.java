package sept.ex_16092024;

public class Lab032_QnA {
    public static void main(String[] args) {
        //int enum = 10;  // enum is keyword can't be used as variable
        //System.out.println(enum);

        int Enum = 10;   // its allowed as its Case sensitive and Enum acts as variable
        System.out.println(Enum);
    }
}
